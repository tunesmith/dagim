# dagim Future Ideas

These ideas are intentionally outside the next focused changeset. They should be revisited after more real use.

## Views

1. Add "why is this blocked?" inspection for manual sequence mode.
2. Add "what would this unblock?" preview for selected nodes.
3. Add subgraph filtering for larger graphs.

## File and Identity

1. Add an interactive selective node ID rekey command for intentionally changing individual stable IDs.
2. Add configurable ID and parent-hint policies if one canonical style proves too rigid.
3. Add merge-conflict assistance for `.dagim` files.
4. Add watch/reload when a file changes outside the TUI.

## Workflow

1. Add persistent completion or processed state if `dagim` proves useful for execution, not just brainstorming.
2. Add optional graph statistics beyond `--check`.
3. Add clipboard integration.
4. Add configurable keybindings.

## Export

1. Add DOT/Graphviz export.
2. Add SVG or PNG graph rendering.
3. Add automatic topological sort export, explicitly labeled as arbitrary among ties.
